[project]
name = "core"
version = "0.1.0"
description = "Python API for DPP4SiPM board communication"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fixedpoint>=1.0.1",
    "numpy>=2.4.6",
    "pyserial>=3.5",
]
