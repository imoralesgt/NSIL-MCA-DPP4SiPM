## Folder distribution
- **fpga** contains all the FPGA-related source files, including the MicroBlaze (uBlaze) embedded soft-processor C code.
- **sw** contains the software used to control the MCA from a computer. Both a Python API (multiplatform) and a full GUI on Microsoft Visual C++ (windows only) are available.

